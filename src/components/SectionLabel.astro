---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="mb-5 flex items-baseline gap-3">
  <h2 class="text-sm font-bold tracking-widest text-foreground">{label}</h2>
  <span class="h-px flex-1 bg-border"></span>
</div>
