---
import { CONFIG } from "../consts";
import Autobattler from "./games/Autobattler.astro";
import ContributionDrawer from "./games/ContributionDrawer.astro";
---

<section id="games" class="bg-background py-14 md:py-18">
  <div class="container mx-auto px-4 sm:px-6">
    <div class="grid gap-8 md:grid-cols-3 md:items-start">
      <div>
        <h2 class="font-headline text-3xl font-bold dark:font-normal">
          {CONFIG.game.title}
        </h2>
        <p
          class="mt-3 max-w-[48ch] text-sm leading-relaxed text-muted-foreground"
        >
          {CONFIG.game.description}
        </p>
      </div>
      <div class="md:col-span-2">
        {CONFIG.game.options.type === "autobattler" && <Autobattler />}
        {
          CONFIG.game.options.type === "contribution-drawer" && (
            <ContributionDrawer />
          )
        }
      </div>
    </div>
  </div>
</section>
